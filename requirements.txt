# Core PSMAF-UNIV development dependencies.
# Keep pip installation compatible with both the original Python 3.10 setup
# and Python 3.12+, where NumPy 1.23.5 is not supported.
numpy==1.23.5; python_version < "3.12"
numpy>=1.26.4; python_version >= "3.12"
torch>=2.0
torchvision>=0.15
timm>=0.9
PyYAML>=6.0
peft>=0.10
pytest>=7.0
